import react from "react";
import { Route, Routes, Navigate } from "react-router-dom";

/**
 * props:
 * - snacks: [{key: STRING, title: STRING, imgUrl: SRING},...]
 */

function RoutesList({ snacks }) {
  return (
    <Routes>
      <Route></Route>
    </Routes>
  );
}
